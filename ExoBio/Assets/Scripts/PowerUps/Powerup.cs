using UnityEngine;
using System.Collections;

//Just a parent class for if we need it that can be "set up"
public class Powerup : MonoBehaviour {

	
	
	// setup whatever the powerup needs to function
	public virtual void Setup (){
		//Set everything up
	}
}
